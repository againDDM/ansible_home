server {
    listen 80 default_server;
    listen [::]:80 default_server;
    root /var/www/static;

    location / {
        try_files $uri $uri/ @redirect;
    }

    location @redirect {
        return 307 https://$host$request_uri;
    }
}
