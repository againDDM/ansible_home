#!/usr/bin/env bash
set -euo pipefail

LOG="{{ duckdns_home }}/duckdns.log"

curl -fsS \
  "https://www.duckdns.org/update?domains={{ duckdns_domains | join(',') }}&token={{ duckdns_token }}&ip=" \
  >> "$LOG"
